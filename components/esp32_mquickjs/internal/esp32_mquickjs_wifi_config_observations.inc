/* Generated by scripts/generate_wifi_config_schema.py. All non-reserved SDK leaves. */
#define WIFI_DRIVER_OBSERVE_STA(NUMBER, BOOLEAN, ENUM, BYTES, SECRET) \
    BYTES("ssidBytes", ssid); \
    SECRET("passwordBytes", password); \
    ENUM("scanMethod", scan_method, scan); \
    BOOLEAN("bssidSet", bssid_set); \
    BYTES("bssidBytes", bssid); \
    NUMBER("channel", channel); \
    NUMBER("listenInterval", listen_interval); \
    ENUM("sortMethod", sort_method, sort); \
    NUMBER("minimumRssi", threshold.rssi); \
    ENUM("minimumAuthMode", threshold.authmode, auth); \
    NUMBER("rssi5gAdjustment", threshold.rssi_5g_adjustment); \
    BOOLEAN("pmfCapable", pmf_cfg.capable); \
    BOOLEAN("pmfRequired", pmf_cfg.required); \
    BOOLEAN("rmEnabled", rm_enabled); \
    BOOLEAN("btmEnabled", btm_enabled); \
    BOOLEAN("mboEnabled", mbo_enabled); \
    BOOLEAN("ftEnabled", ft_enabled); \
    BOOLEAN("oweEnabled", owe_enabled); \
    BOOLEAN("transitionDisable", transition_disable); \
    BOOLEAN("disableWpa3CompatibleMode", disable_wpa3_compatible_mode); \
    ENUM("saePwe", sae_pwe_h2e, pwe); \
    ENUM("saePkMode", sae_pk_mode, pk); \
    NUMBER("failureRetryCount", failure_retry_cnt); \
    BOOLEAN("heDcmSet", he_dcm_set); \
    NUMBER("heDcmMaxConstellationTx", he_dcm_max_constellation_tx); \
    NUMBER("heDcmMaxConstellationRx", he_dcm_max_constellation_rx); \
    BOOLEAN("heMcs9Enabled", he_mcs9_enabled); \
    BOOLEAN("heSuBeamformeeDisabled", he_su_beamformee_disabled); \
    BOOLEAN("heTrigSuBeamformingFeedbackDisabled", he_trig_su_bmforming_feedback_disabled); \
    BOOLEAN("heTrigMuBeamformingPartialFeedbackDisabled", he_trig_mu_bmforming_partial_feedback_disabled); \
    BOOLEAN("heTrigCqiFeedbackDisabled", he_trig_cqi_feedback_disabled); \
    BOOLEAN("vhtSuBeamformeeDisabled", vht_su_beamformee_disabled); \
    BOOLEAN("vhtMuBeamformeeDisabled", vht_mu_beamformee_disabled); \
    BOOLEAN("vhtMcs8Enabled", vht_mcs8_enabled); \
    SECRET("saeH2eIdentifierBytes", sae_h2e_identifier);
#define WIFI_DRIVER_OBSERVE_AP(NUMBER, BOOLEAN, ENUM, BYTES, SECRET) \
    BYTES("ssidBytes", ssid); \
    SECRET("passwordBytes", password); \
    NUMBER("ssidLength", ssid_len); \
    NUMBER("channel", channel); \
    ENUM("authMode", authmode, auth); \
    BOOLEAN("hidden", ssid_hidden); \
    NUMBER("maxConnections", max_connection); \
    NUMBER("beaconIntervalTu", beacon_interval); \
    NUMBER("csaCount", csa_count); \
    NUMBER("dtimPeriod", dtim_period); \
    ENUM("pairwiseCipher", pairwise_cipher, cipher); \
    BOOLEAN("ftmResponder", ftm_responder); \
    BOOLEAN("pmfCapable", pmf_cfg.capable); \
    BOOLEAN("pmfRequired", pmf_cfg.required); \
    ENUM("saePwe", sae_pwe_h2e, pwe); \
    BOOLEAN("transitionDisable", transition_disable); \
    BOOLEAN("saeExt", sae_ext); \
    BOOLEAN("wpa3CompatibleMode", wpa3_compatible_mode); \
    NUMBER("bssMaxIdlePeriod", bss_max_idle_cfg.period); \
    BOOLEAN("bssMaxIdleProtectedKeepAlive", bss_max_idle_cfg.protected_keep_alive); \
    NUMBER("gtkRekeyIntervalSeconds", gtk_rekey_interval);
