# Target-only test modules. Flash/PSRAM mode and heap size remain owned by the
# selected Build Context rather than being guessed from the ESP32-S3 target.
CONFIG_ESP32_MQUICKJS_FEATURE_I2S=y
CONFIG_ESP32_MQUICKJS_FEATURE_CAMERA=y
